import React, { Component } from 'react';
import AlertButton from 'react-bootstrap/lib/Button';

class AlertingButton extends Component {
  render() {
    return (
        <button className="Button" >BootstrapButton</button>
    );
  }
}

export default AlertingButton;
